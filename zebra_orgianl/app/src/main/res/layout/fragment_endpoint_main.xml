<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    style="@style/base_layout_style"
    android:id="@+id/epmain_fragment_container"
    android:orientation="vertical"
    tools:context=".rfidreader.settings.endpoint.EndpointConfigFragment">

    <LinearLayout
        android:id="@+id/epsupportrow"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:layout_marginBottom="10dp"
        android:gravity="bottom">

        <Button
            android:id="@+id/bt_addep"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="10dp"
            android:layout_marginEnd="10dp"
            android:layout_weight="50"
            android:onClick="addNewEpClicked"
            android:text="ADD NEW" />

    </LinearLayout>

    <LinearLayout
        android:id="@+id/epstatusrow"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginTop="30dp"
        android:gravity="bottom">

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/rv_epConfig"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:padding="4dp"
            android:scrollbars="vertical" />

    </LinearLayout>

</LinearLayout>



